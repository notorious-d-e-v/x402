Capped `solana` below 0.40 in the `svm` extra. solana 0.40 removed `solana.rpc.api` and `TxOpts`, which `mechanisms/svm` imports at module scope, so a fresh `pip install x402[svm]` produced an unimportable SVM mechanism.
